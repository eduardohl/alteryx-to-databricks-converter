name: "complex_pipeline_lakeflow"
input: "tests/fixtures/workflows/complex_pipeline.yxmd"
format: lakeflow
expect:
  min_confidence: 80
  max_warnings: 10
  must_contain:
    - "CREATE OR REFRESH"
    - "LIVE."
  must_not_contain:
    - "ERROR"
    - "WITH "
  min_nodes: 2
